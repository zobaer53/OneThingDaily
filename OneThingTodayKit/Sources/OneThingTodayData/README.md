# OneThingTodayData

Phase 2 (persistence) is filled in: `SharedModelContainer`,
`FocusDaySessionRecord`, `ReflectionRecord`,
`SwiftDataFocusSessionRepository`, `SwiftDataReflectionRepository`.

Still to come:

- `AlarmKitSchedulingService` (Phase 3 — scheduling)
- `ActivityKitLiveActivityService` (Phase 4 — Live Activity + the relay engine)
- `FoundationModelsAIService` (Phase 5 — on-device AI)
